# DR-004  用字禁令
date: 2026-08-20    status: ACTIVE
context:   舊命名（Zero*/Router）與被否決的方法綁定，留著會讓 reviewer 誤以為
           方法延續前朝；但 Navigation 一詞仍有未來用途，不該一併燒掉。
options:   全面改名 / 只改方法名 / 不改只在論文避用
ruling:    Zero* 與 Router 全面禁用（含註解與設定檔），改用 Evidence/Selector；
           Navigation 保留，但只能用在真正有獨立 coarse view 時（見 G-07）。
           禁令由測試強制，不靠 code review。
evidence:  commit 043fba0；tests/test_no_banned_deps.py（含逐檔窄例外表 EXEMPT）

# DR-048  SOTA 主表：QPMIL-VL 協定下只跑我方方法
date: 2026-09-04    status: ACTIVE
context:   本機 `/Users/aaron/research/can_dataset` 即 QPMIL-VL 作者公開的四資料集
           benchmark。主路線＝在其協定下只跑我們自己的方法，baseline 引用其發表數字。
           A1 稽核已完成（`docs/DR048_PROTOCOL_AUDIT.md`），**發現兩處實質差異**，
           依 Prompt 5 的紅線「若有差異且不可用只換切分檔的 adapter 對齊 → 停下回報」，
           B / C 段暫停。
options:   見 ruling 中的待裁定事項
           ※ 本行由 Cursor 補上，PI 未原文指定選項清單
ruling:    **ACTIVE —— PI 已於補充裁定回覆兩處差異，B/C 段照裁定執行。**

           **協定（已核實一致的部分，可直接採用）**
           - 資料集：QPMIL-VL 四資料集（TCGA esca / rcc / brca / lung），各 2 子型別，
             8-way label space
           - 切分：`{task}/datasplit/fold_{k}.npz`，k = 1..10，**病人層級**的
             train / val / test 三份
           - 特徵：CONCH，`feats-l1-s256_CONCH/pt_files`，10× 256px patch
           - 載入程式：`data/table_utils.py::read_datasplit_npz` 與
             `retrieve_from_table_clf` 與官方**逐字相同** → 子型別對映由構造一致

           **指標（依 QPMIL-VL 論文定義）**：ACC（average accuracy）、Masked ACC、
           Forgetting、BWT。實作放 `sota/metrics.py`，不與 `run_exp2` 的既有指標混用。

           **報告規則**：全部 fold 如實報告；**無勝負門檻**（不套 DR-020 的三級規則 ——
           那是為 5 seeds 校準的，這裡的變異源是 fold）；**不因結果調設定**。

           **不動 `dr046-freeze`**：DR-046 的結論與產物一律不重算、不覆蓋。
           SOTA 主表是另一條協定線，與 DR-046 的 flat/hier 主線並存不混表。

           **PI 補充裁定（2026-09-04）—— 稽核結論的修正與三點裁定**

           稽核的第 1 點結論**被 PI 更正**：基準論文的 **Tab. 2 就是反向順序**
           （ESCA→RCC→BRCA→NSCLC）且列出全部 baseline；官方 config 裡的 forward
           只是程式預設，不是「唯一的發表協定」。稽核只看了 repo 與評測範本，
           沒有核對論文表格，因此把程式預設誤讀成發表協定。

           1. **順序 = `reverse`（主線）**，對應其 Tab. 2。佇列每個 run 顯式帶
              `--order reverse`。正向（其 Tab. 1）列為**選配**，排在佇列最後。
           2. **平均對象 = 10 折，每折一個 run，`seed = fold` 編號**（fold k → `--seed k`）。
              DR-046 的 5-seed @ fold-1 協定**不動**。報表分兩層：
              `docs/SOTA_TABLE.md`（SOTA 協定）與既有 `docs/DR046_TABLE.md`，
              兩者都帶「不可混讀」警語。
           3. **訓練設定維持本 repo 的：5 epoch、lr 1e-3、rank 4。**
              與基準論文的 12 epoch 不同 —— 這屬**方法設定**，不是協定，
              因此不對齊、但必須在表中寫明。
              8-way 標籤索引順序相反只影響逐類別報表，**ACC 不受影響**。

evidence:  docs/DR048_PROTOCOL_AUDIT.md；scripts/audit_benchmark_protocol.py
           + scripts/report_protocol_audit.py；
           官方 github.com/can-can-ya/QPMIL-VL @ main（2026-09-04 取得）

---

## 執行註記（2026-09-04）

**稽核腳本被自家守門擋下一次，照它的指示重做。**
`tests/test_no_banned_deps.py::test_only_read_only_scripts_may_be_exempt` 規定
**申請禁用字例外的腳本必須是唯讀的**（不得出現 `write_text(` / `mkdir(` / `outputs`）。
第一版的稽核腳本自己寫檔，因此被擋。守門訊息直接給了做法
（「請把唯讀部分拆成獨立模組」），已照做：

- `scripts/audit_benchmark_protocol.py` —— 唯讀，蒐集事實並組裝報告內容；
  含 `qpmil` 字樣，申請窄例外（逐字理由寫在 `EXEMPT`）
- `scripts/report_protocol_audit.py` —— 只負責寫 `docs/DR048_PROTOCOL_AUDIT.md`，
  本身不含任何禁用字，不需例外

AST 確認稽核模組的 import 只有 `numpy` / `pathlib` / `selector` / `sys`，
**沒有任何 QPMIL 模組**。

---

## B 段執行紀錄（2026-09-04）

### B3　`--fold` 支援
`scripts/run_exp2.py` 加 `--fold`（預設 1）。`cfg["fold"]` 本來就是 1，
且產物檔名**只有 `fold != 1` 時**才加 `_f{k}` 後綴 → 預設與加旗標前完全等價，
既有產物一個都不會被覆蓋。

**驗證 `--fold` 真的生效**（否則整個 10 折協定是假的）：
fold 1 與 fold 2 的 `tcga_rcc` 測試集**重疊 0/76**（76 → 81 張），
四個任務的測試片數也不同（rcc 76→81、brca 93→95）。

### B4　指標（`sota/metrics.py`）

| 指標 | 來源 | 可信度 |
|---|---|---|
| ACC | 官方 `manager/manager.py`：`average_test_acc = sum_test_acc / len(dataset_names)` | **核對過** |
| Masked ACC | 官方 `_eval_masked_metrics`：`y_hat[:, label_shift : label_shift + subtype_num]` | **核對過** |
| BWT | **Lopez-Paz & Ranzato (2017)**，GEM（PI 裁定，Prompt 6-3） | 依原始文獻 |
| Forgetting | **Chaudhry et al. (2018)**，Riemannian Walk，**max-based**（PI 裁定，Prompt 6-3） | 依原始文獻 |

    BWT        = mean_{j<T−1} ( A[T−1][j] − A[j][j] )              ← 基準點＝對角線
    Forgetting = mean_{j<T−1} ( max_{l∈[j,T−2]} A[l][j] − A[T−1][j] )  ← 基準點＝峰值

⚠️ 無法從基準論文逐行核對：它正文只引三篇而**沒有印公式**，
官方 repo 也只記錄 forgetting 的**過程**、沒算純量。故依原始文獻，出處由 PI 裁定。

**一致性佐證**：從其論文表抽出 **28 組 (Forgetting, BWT)**，
**每一組都滿足 `Forgetting ≥ |BWT|`，零反例**；多數恰好相等，
少數明顯分離（最極端 ConSlide 的 0.058 vs −0.021）。這正是上面兩式的指紋 ——
基準點不同故恆有 `Forgetting ≥ −BWT`，並在「學完任務 j 之後準確率還會再上升」
時分離，同時**否證**了「Forgetting 直接定義為 −BWT」的簡化讀法。

我們的真實資料**確實落在兩式分離的區間**（A5 seed0：task 0 的峰值在 stage 1 的
0.8667 而非 stage 0 的 0.8000 → Forgetting 0.0762 ≠ −BWT 0.0452），
所以兩個欄位不是重複的。`tests/test_sota_metrics.py` 把這件事釘成測試。

### B5　記憶庫 schema v2
拿掉 `e_t`（[512] float32）與 `B_tilde_t`；`slide_id: str` → `sample_key: int64`。

**為什麼不改變行為**：這兩欄由 `make_entry` 寫入，但**整個 repo 沒有讀取端** ——
`selector/train.py::continual_terms` 只用 `cand_idx / r_old / s_old / u_old`。

**位元等價驗證**：A5 seed0、1 epoch、四個 stage，舊/新 schema 各跑一次，
569 筆 per_slide 記錄**逐位元相同**（sha256 `6c770963db656f0c`）。
產物留存於 `outputs/exp2/b5_old/` 與 `outputs/exp2/b5_new/`。

key ↔ slide_id 的對照表**不進記憶庫**：key 是 `(tau, slide_id)` 的 blake2b 雜湊，
隨時可從 split 的 sid 清單重建（`SampleKeyIndex`）。不用 Python 內建 `hash()` ——
它對 str 有 per-process 隨機化，換個行程就對不回去。

佔用量見 [`docs/MEMORY_FOOTPRINT.md`](../MEMORY_FOOTPRINT.md)（候選滿 256 的最壞情況）：
payload 6,253 → 4,145 B、serialized 8,729 → 6,365 B，
|M| = 512 滿載 4.26 → 3.11 MB。（兩個 schema 都以**欄位字典**量，容器相同才是同一把尺；dataclass 包裝的 64 B 固定開銷 v1 也有，另行單獨報。）

### B6　OPCM-Merge (adapted)

出處：Bui et al., *MergeSlide*, arXiv:2511.13099（WACV 2026），Eq. (3)–(5)；
官方實作 `caodoanh2001/MergeSlide::opcm_mergeslide.py`。
**重新實作，未移植任何一行程式碼。**

**⚠️ 發現一：官方程式的零對角遮罩是 no-op。**
它寫 `projected_task_tv.diag().fill_(0)`，但 `Tensor.diag()` 對 2-D 輸入回傳
**副本**（torch 2.11 實測），不是 view —— 那一行沒有改到被投影的矩陣。
少了遮罩，full SVD 下 `U (Uᵀ Δθ_t V) Vᵀ ≡ Δθ_t`（實測誤差 9e-7），
`G(·)` 退化成恆等，`merge_linear_weights` 與 `merge_other_parameters` 完全相同。

⚠️ **修正先前的簡化說法（2026-09-05）**：本卡原本寫「官方跑出來的是『直接相加』」，
**只對了一半**。遮罩失效讓 `G(·)` 變恆等，但 **λ 正規化仍在做事** ——
官方行為是「直接相加**再整體縮放**到『至今各任務 task vector 範數的平均』」。
張量層實測（seed 0，最終階段）：

| | ‖Δθ̃‖ | 與 C1(sum) 的 cos |
|---|---|---|
| C1（直接相加，無縮放） | 3.3304 | 1 |
| OPCM-nomask（官方行為） | **1.4893** | **1.0000025** |
| OPCM（論文 Alg. 1） | **1.4893** | 0.998352 |

`OPCM-nomask` 與 `C1` 縮放對齊後**最大逐元素差 3.9e-08** —— 兩者確實只差一個純量。
而兩版 OPCM 的**範數完全相同**（λ 把它們都拉到同一個目標範數），
**只差方向** —— 這是對「遮罩到底做了什麼」很乾淨的隔離。
本檔照**論文**實作（用 `.diagonal()`，實測 ⟨G, Δθ̃⟩_F ≈ 0 成立），
並提供 `--no-mask` 重現官方行為。兩者 `tests/test_sota_opcm.py` 都釘住。

**⚠️ 發現二：λ 的分母，論文寫 `‖θ_i‖₂`，官方程式用 `‖θ_i − θ_base‖₂`**
（`get_task_vector_norm`）。本檔照**程式** —— `‖θ_i‖₂` 含未微調的基礎模型本身、
量級由 θ_base 主導，會讓 λ 幾乎不隨任務變化，顯然不是原意。
λ 是**全域純量**（整份 state dict 攤平後取範數），不是逐矩陣算，同樣照程式。

**接法**：θ_base = θ₀，θ_t = θ₀ + Δ_t，Δ_t 直接取自 C1／C2 已算好的
`outputs/exp2/main/dr046_deltas_seed{seed}.pt`（每任務各自從同一個 θ₀ bare 訓練），
正是 OPCM 假設的輸入。⚠️ 因此 OPCM 只在 **fold 1、seed 0–4（DR-046 協定）** 存在，
不是 SOTA 主表的 10 折 —— `docs/SOTA_TABLE.md` 逐列標明。

### B6　zero-shot 參照線
`meanpool`（全部 patch 等權）與 `rand8`（隨機 B 個，與主線同預算），共用凍結的
CONCH 頭，**沒有可訓練參數** → 跨 stage 完全不變，Forgetting 與 BWT 必然為 0。
這不是「不遺忘的好方法」而是「沒有東西可遺忘」，表中標成參照線而非 CL 方法。
冒煙實測 Forgetting = 0.000，符合預期。

**`meanpool` 的記錄不帶 `B` / `selected_idx` / `weights_*`（2026-09-04 修正）。**
第一版照抄選取類的欄位寫，被 `tests/test_per_slide_records.py` 抓到兩個問題：

1. **權重歸不了一**：`round(1/n, 6)` 在 n 幾千時累積誤差達 **1.7e-3**
   （n=4808 → 0.000208 × 4808 = 1.001728），超過測試的 1e-3 容差。
2. **量體**：`selected_idx` 就是 `range(n)`（n 最大 8466）、權重是 n 份相同的數，
   **單折 57 MB**，十折 570 MB 全是冗餘。

處置不是放寬測試，而是**改正分類**：meanpool 沒有預算、也沒有做選取，
本來就不該算「選取類評估」。`test_per_slide_records._is_selection_dump` 正是
以 `B` 欄位的有無判定，並註明沒有 patch 選取這回事的只需滿足 REQUIRED ——
拿掉 `B` 是照它既有的語意走，不是繞過它。改帶顯式的 `pooling` 欄位
（`"all"` / `"random"`），`n_patch` + `pooling` 已完整說明發生什麼事。
產物 **57 MB → 279 KB**。

⚠️ **突變檢查**：把 `B` 與那些欄位加回去（＝修正前那一版），
`test_per_slide_records` 立刻失敗（`softmax 權重未歸一`）——
確認守門仍然有效，不是被繞過。

### B7　`docs/SOTA_TABLE.md`
`sota/report_sota.py` 產生，只放數字不寫解讀（同 `report_dr046_gates.py` 的規矩）。
帶「不可混讀」警語、逐列協定註記、逐列溯源（哪些 fold/seed）。
**外部方法的數字一律留空**由 PI 填 —— 腳本刻意不從論文抄，
抄進來的欄位會與我們自己重算的欄位長得一模一樣，讀者無法分辨。

### B8　冒煙
A5 flat `--fold 2 --epochs 1 --max-train 8`、`sota/zeroshot.py --fold 2`、
`sota/opcm.py --seeds 0` 三項全過，端到端產出 SOTA 表；產物已刪。

### 治理修正
`tests/test_no_banned_deps.py` 的 `SCANNED_DIRS` 原本**不含 `sota/`** ——
PI 的紅線「sota/ 內亦不得引入 QPMIL 程式碼」在此之前沒有被任何測試強制。
已加入。加入後立刻抓到一個真實違例：B3 的 `--fold` help 字串指名了該方法，已改寫。


---

## Prompt 6 執行紀錄（2026-09-05）

### 0　ZS 產物
fold 10 的 57 MB 舊格式版**上一輪（commit `a682246`）已刪除並以新 schema 重產**。
本輪核對十折：`ZS-mean` 與 `ZS-rand8` 各 10 折，**欄位集合各只有 1 種**，
`B` / `pooling` / 權重歸一 **0 個問題**。

>5 MB 的檔案共 6 個，**全部是早已追蹤的舊產物**（DR-046 的 5 個 delta 快取各 6.0 MB、
`outputs/exp1/stage1/results.json` 5.0 MB），本輪一個新檔都沒有超標
（最大的新產物是 `A5_main_seed4_f4.json` 799 KB）。
因此**沒有新增 `.gitignore` 規則** —— `.gitignore` 開頭的既有註解已明訂
「大型產物規則針對未來新增的，已在歷史裡的維持追蹤」，
對已追蹤檔加規則不會移除它們，只會製造誤解。

改為加**常設守門** `tests/test_product_sizes.py`：逐 slide 產物 ≤ 5 MB、
全 repo 不得有新的 >5 MB 檔案（既有 6 個逐一列在 `GRANDFATHERED` 並驗證它們
確實超標）。選 test 而不選 `.gitignore` 的理由：後者會讓超大產物**靜默消失**，
更難發現寫錯了什麼。突變檢查：放一個 5.3 MB 的假產物進去，兩條測試立刻失敗。

### 1　外部十二列
`sota/external_baselines.py`（**唯讀資料模組**，不寫檔）收 QPMIL-VL Tab. 2
（reverse、10 折）十二列，出處欄 `[gou2025qpmil], Tab. 2`（bibtex key 見
`paper/refs.bib:149`）。含被禁識別字，於 `EXEMPT` 開窄例外。

同時把 `test_only_read_only_scripts_may_be_exempt` 的**唯讀規則從 `scripts/`
擴到 `sota/`** —— `sota/` 同樣是產出結果的 pipeline，原本有缺口。
突變檢查：在該模組加一行寫檔，規則立刻擋下。

十列有 Forgetting/BWT 的數字與 2026-09-04 從論文 HTML 抽出的 28 組**逐字對得上**。

### 2　OPCM 兩版
`--no-mask` 另存為 **`OPCM-nomask`**（arm 名不同，否則 `report_sota.load_runs`
以 `(arm, arch)` 為 key 會把兩版併成同一列 —— 這個 bug 在產表前抓到並修掉）。

fold 1 / seed 0–4，配對差（`OPCM-nomask − C1(sum)`）：

| 指標 | mean | win |
|---|---|---|
| ACC | **+0.0136** | 4/5 |
| Masked ACC | +0.0131 | 4/5 |
| Forgetting | +0.0262 | 4/5（C1 較低） |
| BWT | −0.0324 | 0/5（C1 較高） |

亦即**單靠 λ 的範數正規化**（方向與 C1 完全相同）就讓 ACC 高了 1.36 pp，
但遺忘也變多。`OPCM − OPCM-nomask`（遮罩的淨效果）四個指標都是 ≤3/5，
**在噪音內**（ACC mean +0.0033）。

### 3　指標出處
依 PI 裁定改標為 Lopez-Paz & Ranzato (2017)（BWT）與
Chaudhry et al. (2018) max-based（Forgetting）。見上方指標表。

### 4　特徵記錄大小
四 task 全部 **3,282 張** slide，N 由特徵檔大小反推
（`(size − 747) / (512×4)`，整除性逐檔驗證，不載入 19.5 GB 張量）：

| | N 平均 | N 中位數 | 記錄平均 | 記錄中位數 |
|---|---|---|---|---|
| 四 task 合計 | 3,123 | 3,060 | **6.10 MB** | **5.98 MB** |

與 |M| = 512 的記憶庫 **3.11 MB** 並列：整個記憶庫約等於 **51% 張** slide 的特徵；
全部 3,282 張 slide 的特徵合計 19.5 GB，記憶庫佔 **0.02%**。

### 5　產表
`docs/SOTA_TABLE.md`；`verify_doc_numbers.py` 的 `PAPER_ARTIFACTS` 納入
`docs/SOTA_TABLE.md`、`docs/MEMORY_FOOTPRINT.md`、`outputs/exp2/sota/EXP2.md`。

---

## Prompt 7：佇列收尾（2026-09-05）

### 佇列完成
`logs/sota_queue.log` 末行：**「62 步，失敗 0 個」**，
2026-09-04 17:20:24 起、2026-09-05 12:37:40 止，**共 19 小時 17 分**。
`logs/sota/FAILED.txt` 存在但 **0 bytes** —— 62 步全部 exit 0（含最後的 `report_sota`；
佇列的 `run()` 只在非零結束時寫入該檔）。

產物清點 **80 個檔、9 個組合全滿、清單外組合 0**：

| arm | order | arch | 折數 |
|---|---|---|---|
| A5 | reverse | flat / hier | 10 / 10 |
| A5 | **main（forward）** | flat | 10 |
| A1、A3 | reverse | flat | 10、10 |
| ZS-mean、ZS-rand8 | reverse | flat | 10、10 |
| OPCM、OPCM-nomask | reverse | flat | 5、5（fold 1，DR-046 協定） |

### 選配段三列
`docs/SOTA_TABLE.md` 新增 A1、A3（flat reverse）與 A5 flat **forward**。

⚠️ **forward 是不同的實驗，不是同一列的變體**：`report_sota.load_runs` 的 key
改為 **(arm, arch, order)**，每一列用**自己 order 的 task 序**算指標。
誤用 reverse 的序不會靜默算錯，而是讓該 run **整個算不出來被跳過**
（reverse 的第一個任務 ESCA 在 forward 記錄裡只出現在最後一階段，
Forgetting 需要的「最終階段之前的峰值」是空的 → `all_metrics` 拋 ValueError）。
`tests/test_sota_report.py` 端到端釘住這件事；四個突變（三軸一律數正號、
order 不進 key、配對不取交集、列用主 order 的 task 序）全部抓到。

**forward 的外部數字本輪不填** —— PI 未指定，腳本不自行從論文擷取。

### 配對統計的報告慣例
逐折同折相減，**只報 k/10 的折數與均值差，不貼三級標籤**。

理由（寫進報表本文）：**DR-020 的三級規則是為 5 個 model seed 校準的**，
這裡的變異源是**資料切分**，沒有重新校準過，套用會是誤用。
這與本卡 ruling 早先寫的「無勝負門檻（不套 DR-020 的三級規則）」一致。

「較佳折數」**帶方向**：ACC / Masked ACC 越大越佳、Forgetting 越小越佳 ——
三軸都以「A 較佳」計數，不是一律數正號（這是最容易寫錯的一條，
`test_paired_counts_forgetting_the_other_way_round` 專門守它）。

---

## 未預註冊之觀察：階層優勢隨任務順序反轉（2026-09-06）

### 觀察
`hier − flat`（A5，逐折同折配對，10 折）在兩個任務順序下**方向相反**：

| 順序 | ACC 均值差 | hier 較佳的折數 |
|---|---|---|
| **reverse**（其 Tab. 2；本 repo 主線） | **+0.0247** | **8/10** |
| **forward**（其 Tab. 1） | **−0.0295** | **1/10** |

兩個順序都是**完整十折**，不是資料不足。forward 下逐折看，只有 fold 6 一折 hier
較佳（+0.020），其餘九折全部落後，最差 −0.086。
另兩軸（forward）：Masked ACC −0.0156（4/10）、Forgetting +0.0063（6/10）。

逐折數字在 [`docs/SOTA_TABLE.md`](../SOTA_TABLE.md) 的配對小節。

### 這是**未預註冊**的
DR-046 的階層決定是在 **reverse 順序**上做的（`--order reverse` 是主線，
gate 表、三級規則、DR-042 的 flat/hier 比較全部在該順序下校準）。
**forward 從來不是預註冊比較的一部分** —— 它是 DR-048 為了對齊外部協定
才補跑的選配段。因此本節是觀察，不是 gate 的判定。

### PI 裁定（照錄）
1. **如實報告兩個順序。** 不挑對自己有利的那一個，也不把 forward 藏進附錄。
2. **方法不改。**
3. **階層維持為「正典順序上的預註冊選擇」** —— 它在 reverse（預註冊的順序）
   上通過，這個地位不因一個未預註冊的順序而改變；但論文必須同時揭露 forward
   的反向結果。

⚠️ 依本卡既有慣例，`docs/SOTA_TABLE.md` **只放數字不寫解讀**；
上面的判讀文字以本卡與論文為準。

---

## 過夜佇列 0906：正向補鏈完成 ＋ 重現檢查（**待 PI 裁定**）

佇列 22 步、**失敗 0 個**（2026-09-06 01:32:41 → 06:51:17，5h19m）。
產物 **110 檔、12 個組合全滿**（A5 flat/hier × reverse/forward、A1/A3 flat ×
reverse/forward 各 10 折；ZS 兩種 10 折；OPCM 兩版 5 seed）。

⚠️ 以下**只記事實，不含裁定** —— 兩項都需要 PI 判讀。

### 觀察一：`A5 − A3` 的優勢在正向幾乎消失

| 順序 | ACC 均值差 | A5 較佳的折數 | Masked ACC | Forgetting |
|---|---|---|---|---|
| reverse | +0.0082 | 7/10 | −0.0054（4/10） | −0.0244（8/10） |
| **forward** | **−0.0004** | **5/10** | −0.0055（3/10） | −0.0093（8/10） |

正向下兩臂的 ACC 都是 **0.841**（A5 0.841 ± 0.043、A3 0.841 ± 0.019），
逐折 5 勝 5 敗。亦即**在正向順序上，KD ＋ equilibrium 兩個成分對 ACC 沒有增益**；
只有 Forgetting 保住 8/10 的小幅優勢。

這與已落帳的「階層優勢隨順序反轉」是**同一類**的順序依賴，但對象不同：
前者是底盤（hier vs flat），這一項是方法成分（A5 vs A3）。
逐折數字在 [`docs/SOTA_TABLE.md`](../SOTA_TABLE.md) 的配對小節。

⚠️ 與 hier 那一項相同，**forward 不是預註冊比較的一部分**。

### 觀察二：外部基準的重現檢查未成立，且理由**不足以支持「不可行」**

`scripts/repro_external.py` 依規格 clone 到 repo 之外（`~/research/ext/`）、
指向本機資料集、跑 smoke。smoke 在 **3 秒內以 returncode=1 失敗**：

    ModuleNotFoundError: No module named 'wandb'

腳本照規格寫下 `logs/sota/QPMIL_REPRO_INFEASIBLE.txt`，但**那個「不可行」的
結論是不成立的** —— 它只證明本機少裝套件，沒有證明對方的程式在這台機器上
跑不起來。純相依探測顯示缺三個：**`wandb`、`einops`、`timm`**，都是一般套件。

真正的可行性問題**還沒被測到**：本機 `cuda=False / mps=True`，而對方設定是
`cuda_id: 0`。要得到有意義的結論，必須先補齊相依、再看它是否能在 MPS／CPU 上跑。

**因此本項的正確狀態是「未完成」，不是「不可行」**；`INFEASIBLE` 檔名沿用規格，
內容已逐字記錄真實原因。待 PI 決定是否補裝套件重跑。

---

## 外部基準的重現檢查：完成（2026-09-07）

以**對方公開的實作**（`can-can-ya/QPMIL-VL` @ `3a7a769`）在 RunPod RTX 4090 上跑
reverse、其預設 12 epoch、同一份 10 折切分。指標由**它自己輸出的準確率矩陣**
（`metrics/test_acc.txt`）套**我們統一的定義**算出（`sota/repro_metrics.py`）。

| 指標 | 重現（10 折） | 發表值 Tab. 2 | 差 |
|---|---|---|---|
| ACC ↑ | **0.814 ± 0.065** | 0.859 ± 0.032 | **−0.045** |
| Masked ACC ↑ | 0.908 ± 0.043 | 0.925 ± 0.018 | −0.017 |
| Forgetting ↓ | **0.147 ± 0.091** | 0.064 ± 0.031 | **+0.083** |
| BWT ↑ | −0.147 ± 0.091 | −0.064 ± 0.031 | −0.083 |

⚠️ **這十折全部滿足 `Forgetting == −BWT`**（逐折逐位元相等）。代表每個任務的
準確率峰值都恰好落在剛學完它的那一階段、之後不再上升 —— 兩個定義只在
「學完之後還會再升高」時才分離。**對照：我們自己的 A5／flat／reverse 是
Forgetting 0.090 而 −BWT 0.082，確實分離。** 這是兩個方法在遺忘形態上的
一項實質差異，不是計算重複。

逐折 ACC 0.729–0.900，**標準差約為發表值的兩倍**。

### ⚠️ 這個結果**不能**讀成「原論文數字有誤」

四個限定，缺一不可（同樣寫在 `docs/SOTA_TABLE.md` 該節）：

1. 這是**我們**在別的軟硬體上跑它的公開實作，不是作者原始環境的重跑
   （torch 2.4.1 vs 其釘的 1.11、GPU 型號、numpy 版本皆不同）。
2. **reverse 順序需要重排 `class_ensemble.json` 的鍵序才跑得起來** ——
   其 `get_current_ensemble_classes` 依該檔鍵序決定任務順序、**完全不看 config 的
   `dataset_names`**。若其發表的 Tab. 2 是用我們沒拿到的另一條路徑產生，
   這裡的設定就未必等同。**這是本次最實質的發現**：其釋出的程式碼把任務順序
   寫死在資料檔裡，reverse 不是開箱可跑的。
3. 另套用一道 API 相容修補（`np.long` → `np.int64`，語意等價）。
   GPU 路徑只需要這一道；CPU 路徑另需兩道（見 `docs/repro/SHIMS.md`）。
4. **裝置一致性對照無法取得** —— 同折的 CPU 對照在 16 GB 的 Mac 上跑到第三個
   任務即被系統終止（OOM，74 分鐘、無 traceback、leaked semaphores）。
   因此無法分離「裝置」與「其餘環境差異」的貢獻。
   兩邊的 1-epoch smoke 雖都完成，但那個層級被 run-to-run 變異主導
   （第一格皆 0.933333、其後即分歧），**不足以當作裝置對照**，故不採用。

### 可以支持的結論
「在這個環境下，用其公開程式碼與同一份切分，得到的數字低於發表值且變異約兩倍。」

### 執行紀錄
* pod：RTX 4090 24 GB、$0.740/hr。8 折並行批次牆鐘 **61 分**，fold 7 重跑 894 秒。
* 資料**未上傳** —— pod 的網路磁碟區早已有同一份 20 GB 資料集（逐 task 檔數與本機一致）。
* 兩個中途故障：pod 資料集的 `tcga_brca/datasplit/fold_7.npz` 是 0 bytes（補傳後重跑，
  另已驗證其餘 39 個切分檔正常）；fold 10 因**我們**給每折不同的 `--time`
  而在其跨折彙總步驟崩潰，但該折自身指標已完整產出，不需重跑。
* 環境重建手順與修補清單：[`docs/repro/RUNPOD.md`](../repro/RUNPOD.md)、
  [`docs/repro/SHIMS.md`](../repro/SHIMS.md)。

---

## 正向重現（其原生順序，2026-09-07）

**這一輪是最乾淨的重現條件**：正向是他們的原生順序，所以
`class_ensemble.json` 原樣不動、`dataset_names` 等順序欄位原樣不動、
12 epoch 原樣不動。**唯一偏離其釋出狀態的只有 4 個本機路徑與一道語意等價的
API 修補**（`np.long` → `np.int64`）。

| 指標 | 重現（10 折） | 發表值 Tab. 1 | 差 |
|---|---|---|---|
| ACC ↑ | **0.884 ± 0.033** | 0.890 ± 0.021 | **−0.006** |
| Masked ACC ↑ | 0.926 ± 0.025 | （待自論文補） | — |
| Forgetting ↓ | 0.038 ± 0.018 | （待自論文補） | — |
| BWT ↑ | −0.038 ± 0.018 | （待自論文補） | — |

逐折 ACC 0.834–0.934。

### 與反向那一輪並置，結論才成立

| | 改動 | ACC 重現 | 發表值 | 差 | 逐折 sd |
|---|---|---|---|---|---|
| **forward**（原生） | 路徑 + 1 shim | 0.884 | 0.890 | **−0.006** | 0.033 |
| **reverse** | 路徑 + 1 shim **+ 重排 `class_ensemble.json` 鍵序** | 0.814 | 0.859 | **−0.045** | 0.065 |

**正向幾乎完全對上（差 0.6 個百分點、變異同量級）；反向差 4.5 個百分點且變異約兩倍。**

這強烈支持先前那個發現的重要性：**其釋出的程式碼把任務順序寫死在
`class_ensemble.json` 的鍵序裡**（`get_current_ensemble_classes` 依該檔鍵序累積
類別、完全不看 config 的 `dataset_names`）。我們為了跑 reverse 而重排該檔，
**很可能不等同他們產生 Tab. 2 的方式**。

### 因此可以說 / 不可以說

✅ **可以說**：其公開實作在**原生正向順序**下可以重現到發表值的 0.6 個百分點以內；
   我們的執行程序、資料與指標定義因此獲得交叉驗證。
✅ **可以說**：其公開實作**不支援開箱跑反向順序**；要跑必須改動資料檔。
❌ **不可以說**：其 Tab. 2 的數字有誤。我們的反向設定是自行推斷的重排，
   不是他們的路徑；差距的來源無法歸因。

### 執行紀錄
* 舊 pod `jnd934wacl9pk6` 無法重啟（`not enough free GPUs on the host`）——
  停機時 GPU 被釋出後遭他人取用。PI 另開 `uh30ur1kz1suoh`
  （`unemployed_scarlet_macaw`）並掛同一個網路磁碟區，**資料集、含 shim 的 repo、
  cfg、前一輪 results 一個位元都沒少**，只需重裝容器層的 pip 套件（約 2 分鐘）。
  `docs/repro/RUNPOD.md` 的重建手順在真實情境下驗證有效。
* folds 1–9 三折並行，fold 10 最後單獨跑（讓其跨折彙總讀得到前九折）——
  上一輪 fold 10 崩潰的坑因此避開，**10/10 全部成功**。
* 逐折 1284–1518 秒；fold 10 單獨跑 919 秒。牆鐘約 1 小時 26 分。
* 結果取回後 pod 已 stop。

---

## 重現檢查完成（兩個順序，2026-09-08）

補齊 Tab. 1 的發表值後，**正向四個指標全部對上**：

| 指標 | 重現（10 折） | 發表值 Tab. 1 | 差 |
|---|---|---|---|
| ACC ↑ | 0.884 ± 0.033 | 0.890 ± 0.021 | **−0.006** |
| Masked ACC ↑ | 0.926 ± 0.025 | 0.930 ± 0.018 | **−0.004** |
| Forgetting ↓ | 0.038 ± 0.018 | 0.027 ± 0.014 | **+0.011** |
| BWT ↑ | −0.038 ± 0.018 | −0.027 ± 0.014 | **−0.011** |

**四項全在一個百分點以內。** 正向的重現條件是：其原生順序、原生資料檔、
其預設 12 epoch，**唯一偏離是 4 個本機路徑與一道語意等價的 API 修補**。

### ⚠️ 反向的差距現在有具體成因（本輪新發現）

PI 提供 Tab. 1 發表值時一併給出其設定：**mini-batch 正向 16／反向 8**。
本 repo 兩輪都用其 config 的預設值 **16** —— 也就是說**反向那一輪的 mini-batch
用錯了**（16 而非 8），當時無從得知這個差別。

反向因此有**兩處已知偏離**，都在我們這一側：

1. 重排 `class_ensemble.json` 的鍵序（否則跑不起來）
2. **mini-batch 16 而非 8**

| | 改動 | ACC | 發表值 | 差 |
|---|---|---|---|---|
| forward | 路徑 + 1 shim | 0.884 | 0.890 | −0.006 |
| reverse | 路徑 + 1 shim + 重排鍵序 + **mini-batch 錯用 16** | 0.814 | 0.859 | −0.045 |

### 可說／不可說（三條）

✅ **可以說**：其公開實作在**原生正向順序**下可重現到發表值**一個百分點以內**
   （四個指標皆然）。我們的執行程序、資料、指標定義因此獲得交叉驗證 ——
   這也反過來支持我們自己那張表的可信度。

✅ **可以說**：其公開實作**不支援開箱跑反向順序** ——
   `get_current_ensemble_classes` 依 `class_ensemble.json` 的鍵序決定任務順序、
   完全不看 config 的 `dataset_names`，必須改動資料檔才能跑。

❌ **不可以說**：其 Tab. 2（反向）的數字有誤。我們反向那一輪有兩處自己造成的
   偏離（重排鍵序、mini-batch 錯用），**差距的來源無法歸因於原論文**。
   若要驗證反向，須以 `bp_every_batch: 8` 重跑，並確認鍵序重排等同其做法。

### 未完成事項
* 反向以 `bp_every_batch: 8` 重跑（未執行；需 PI 裁定是否值得再開 pod）。
* 裝置一致性對照仍無法取得（Mac CPU 同折 OOM）。

---

## 反向重現以其論文 batch 重跑：差距消失（2026-09-08）

前輪反向用了程式預設的 `bp_every_batch: 16`，而其論文反向報的是 **8**。
以 **8** 重跑十折（其餘設定與前輪**逐行相同**）：

| 指標 | batch 16（前輪） | **batch 8（本輪）** | 發表值 Tab. 2 | 本輪差 |
|---|---|---|---|---|
| ACC ↑ | 0.814 ± 0.065 | **0.868 ± 0.046** | 0.859 ± 0.032 | **+0.009** |
| Masked ACC ↑ | 0.908 ± 0.043 | **0.935 ± 0.018** | 0.925 ± 0.018 | **+0.010** |
| Forgetting ↓ | 0.147 ± 0.091 | **0.064 ± 0.054** | 0.064 ± 0.031 | **−0.000** |
| BWT ↑ | −0.147 ± 0.091 | **−0.064 ± 0.054** | −0.064 ± 0.031 | **+0.000** |

**Forgetting 幾乎逐位元對上（0.0638 vs 0.064）。** 四個指標全部落在一個百分點以內。

### 這解決了先前懸而未決的歸因

先前 DR-048 把反向的 −0.045 列為「有兩處我方偏離、無法歸因」。現在確定：

* **差距的主因就是 mini-batch 未對齊**（16 而非 8）。
* **重排 `class_ensemble.json` 鍵序是正確的做法** —— 它重現得出其發表的反向結果。
  這件事先前只能存疑，現在有正面證據。

### 因此「可說」的範圍擴大了

✅ 其公開實作在**兩個順序**下都可重現到發表值一個百分點以內
   （forward 四指標 ≤0.011；reverse-b8 四指標 ≤0.010）。
✅ 我們為跑反向所做的鍵序重排**經驗證等同其結果**，不是臆測。
✅ 其公開實作仍**不支援開箱跑反向**（順序寫死在資料檔），且**其 config 預設的
   mini-batch 16 與論文反向所報的 8 不一致** —— 照預設跑會偏低 5.4 個百分點。
   這對後續想重現該工作的人是實用的資訊。
❌ 先前那條「不可說其 Tab. 2 有誤」**依然成立且更強** —— 差距已完全由我方設定
   解釋，原論文數字在本次檢查下獲得支持。

### ⚠️ 一個未排除的變數
本輪跑在 **A100-SXM4-80GB**，前兩輪是 RTX 4090，故嚴格說 batch 與 GPU 同時變動。
但差距朝發表值移動 **5.4 個百分點**，遠大於一般 GPU 數值差異的量級，
歸因於 batch 是合理的；若要完全排除，須在同一張卡上跑 b16／b8 對照（未執行）。

### 執行紀錄
* pod `fazrkhdokjjewd`（A100 SXM 80GB），掛同一網路磁碟區，前兩輪資產完整。
* 逐折 1895–2652 秒（batch 8 比 batch 16 慢，符合預期）；fold 10 單獨 884 秒。
  牆鐘約 2 小時 9 分。**10/10 全部成功。** 結果取回後 pod 已 stop。
* 產物獨立標籤 `reverse_b8` / `--time revb8` / `logs_revb8`，未覆蓋前輪。

### ⚠️ 稿件需同步
`paper/main.tex` v0.89a 的主表 caption 目前寫「our reverse rerun used the code's
default mini-batch of 16 where the paper reports 8」。本輪之後應改為「兩種 batch
都跑了，以其論文設定（8）重現的結果與發表值相符」。**待 PI 出下一版。**

---

## 設定事實：八類 cosine 頭自第一個任務起固定，無類別累積（2026-09-08）

比較外部協定時必須連帶說明的一項**設定差異**。這不是實驗結果，
可由程式碼逐行核對。

### 本方法
`L_diag` 的 logits **自始至終涵蓋全部 8 個類別**：

* `run_exp2.Ctx.f_txt` = 四個 task 的 class-text 特徵串接 → `(8, 512)`，
  建構一次後不再改變（`scripts/run_exp2.py:147`）
* 訓練呼叫傳的就是這個完整 8-way（`scripts/run_exp2.py:219`；
  `selector/train.py:208` 註解寫明「8-way，兩種模式共用」）
* `frozen_head` → `logit_scale * (pooled @ f_txt.t())`，8 個 logits
* `l_diag` 對這 8 個 logits 做 cross-entropy，target 是**全域**標籤
  （位移 `2 × task_pos`：esca→0/1、rcc→2/3、brca→4/5、lung→6/7；已實測確認）

**「至今所有類別」與「全部類別」在此不同，我們是後者** —— 訓練第一個任務
（ESCA）時，logits 已涵蓋另外三個 task 的 6 個尚未見過的類別。

### 外部基準
`get_current_ensemble_classes` 依 `class_ensemble.json` 的鍵序**逐步累積**、
遇到當前 dataset 就 `break` —— 訓練任務 τ 時 logits 只涵蓋**至今看過**的類別。
（同一個函式也是「任務順序寫死在資料檔」那個發現的來源。）

### 這對比較的意義
兩者的 **class-IL 難度不同**：固定 8-way 從第一個任務起就要對抗全部干擾類別，
累積式則逐步變難。

⚠️ **這是設定差異，不是效能主張。** 不得寫成「我們的方法更強」或
「我們的設定更公平」；**只能寫成「兩者的 class-IL 難度不同，比較時須註明」**。
Masked ACC（task-IL）不受此影響 —— 它在評估時才切片，兩邊都只看該任務自己的類別。

### 為什麼沒有累積機制
CONTRACT-4 的凍結 CONCH 頭，其 class-text 特徵是**預先算好的常數**，
沒有隨任務成長的參數。累積式需要一個會長大的分類頭，那與 CONTRACT-4 相斥。

對應宣稱：`docs/CLAIMS.md` C-30。凍結頭的歸因後果另見 C-22 與 DR-012。

# DR-048  SOTA 主表：QPMIL-VL 協定下只跑我方方法
date: 2026-09-04    status: PARKED
context:   本機 `/Users/aaron/research/can_dataset` 即 QPMIL-VL 作者公開的四資料集
           benchmark。主路線＝在其協定下只跑我們自己的方法，baseline 引用其發表數字。
           A1 稽核已完成（`docs/DR048_PROTOCOL_AUDIT.md`），**發現兩處實質差異**，
           依 Prompt 5 的紅線「若有差異且不可用只換切分檔的 adapter 對齊 → 停下回報」，
           B / C 段暫停。
options:   見 ruling 中的待裁定事項
           ※ 本行由 Cursor 補上，PI 未原文指定選項清單
ruling:    **PARKED —— A1 稽核完成，B/C 段待 PI 對兩處協定差異裁定後才啟動。**

           **協定（已核實一致的部分，可直接採用）**
           - 資料集：QPMIL-VL 四資料集（TCGA esca / rcc / brca / lung），各 2 子型別，
             8-way label space
           - 切分：`{task}/datasplit/fold_{k}.npz`，k = 1..10，**病人層級**的
             train / val / test 三份
           - 特徵：CONCH，`feats-l1-s256_CONCH/pt_files`，10× 256px patch
           - 載入程式：`data/table_utils.py::read_datasplit_npz` 與
             `retrieve_from_table_clf` 與官方**逐字相同** → 子型別對映由構造一致

           **指標（依 QPMIL-VL 論文定義）**：ACC（average accuracy）、Masked ACC、
           Forgetting、BWT。實作放 `sota/metrics.py`，不與 `run_exp2` 的既有指標混用。

           **報告規則**：全部 fold 如實報告；**無勝負門檻**（不套 DR-020 的三級規則 ——
           那是為 5 seeds 校準的，這裡的變異源是 fold）；**不因結果調設定**。

           **不動 `dr046-freeze`**：DR-046 的結論與產物一律不重算、不覆蓋。
           SOTA 主表是另一條協定線，與 DR-046 的 flat/hier 主線並存不混表。

           ⚠️ **待 PI 裁定的兩件事（B/C 段的前提）**：
           1. **任務順序**。官方發表協定是 **forward（lung→brca→rcc→esca）**，
              repo 內唯一的評測範本是 `eval_template/forward-order.xlsx`。
              本 repo 主線是 `reverse`。要引用其發表數字，佇列必須跑 `--order main`。
              Prompt 5 的佇列未指定 order，預設會跑成 `reverse`。
           2. **平均對象**。官方對 **10 個 fold** 平均（`main.sh` SEED 1..10 →
              `data_split_seed` → `fold_{k}.npz`）；本 repo 主線對 **5 個 model seed
              @ fold 1** 平均。SOTA 主表應改為對 fold 平均。
evidence:  docs/DR048_PROTOCOL_AUDIT.md；scripts/audit_benchmark_protocol.py
           + scripts/report_protocol_audit.py；
           官方 github.com/can-can-ya/QPMIL-VL @ main（2026-09-04 取得）

---

## 執行註記（2026-09-04）

**稽核腳本被自家守門擋下一次，照它的指示重做。**
`tests/test_no_banned_deps.py::test_only_read_only_scripts_may_be_exempt` 規定
**申請禁用字例外的腳本必須是唯讀的**（不得出現 `write_text(` / `mkdir(` / `outputs`）。
第一版的稽核腳本自己寫檔，因此被擋。守門訊息直接給了做法
（「請把唯讀部分拆成獨立模組」），已照做：

- `scripts/audit_benchmark_protocol.py` —— 唯讀，蒐集事實並組裝報告內容；
  含 `qpmil` 字樣，申請窄例外（逐字理由寫在 `EXEMPT`）
- `scripts/report_protocol_audit.py` —— 只負責寫 `docs/DR048_PROTOCOL_AUDIT.md`，
  本身不含任何禁用字，不需例外

AST 確認稽核模組的 import 只有 `numpy` / `pathlib` / `selector` / `sys`，
**沒有任何 QPMIL 模組**。

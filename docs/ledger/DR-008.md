# DR-008  q_τ 定義凍結（該 task 全部 class-text 特徵平均）
date: 2026-08-21    status: ACTIVE
context:   q_τ 若改成可學習的 task embedding，會退化成 one-hot 的連續版，
           「文字語意不只是 task ID」這個主張就自動失效。
options:   class-text 平均（凍結）/ 可學習 task embedding / 不用 q_τ
ruling:    定義凍結為 normalize(mean_c f_txt[c])，變更需 PI 批准；不可學習化。
           label leakage 由雙 assert 把關（簽名無 label 參數 + 同 task 跨 label 位元相同）。
           ⚠️ **定義 ACTIVE，但「task-conditioning 有效」這個主張已 PARK → G-05**
           （S1 顯示跨器官 task 98.2/98.6% 線性可分，q 結構性冗餘）。
evidence:  selector/task_query.py（🔒 凍結註記）；tests/test_leakage.py；
           outputs/exp1/diag/TASK_SEPARABILITY.md；commit eea09ce

# DR-025  配額口徑改為對整個 budget；chunked loop 在無 state 下為 no-op
date: 2026-08-25    status: ACTIVE
context:   G1 的階層版顯著劣於 flat，但診斷顯示那不是階層的問題：per-chunk 配額
           在 c=1 時 largest-remainder 只有一個名額可發、必然給 argmax(r)，
           r 逐輪不變 ⇒ 每輪同一組 ⇒ 退化為「先挑一組再取該組 top-8」
           （84.5% 的 slide 只用一組）。CONTRACT-1 的 per-chunk 口徑是設計錯誤。
options:   (a) 維持 per-chunk、接受階層失敗 (b) 改為對整個 budget 配額並重跑 G1'
ruling:    (b)。配額改對剩餘預算/總預算（累計等價形式），逐輪追蹤各組已取數，
           取「尚未滿額且 r_j 最高」的組。B=8、c=1 不變（DR-005 不動）。
           DR-021 的判準原文沿用、一字不改 —— 這是修復壞掉的實驗，不是重釣結果。
           DR-021 的裁定 (a) 未被否證（G1 測到的不是階層），維持 ACTIVE，由 G1' 重驗。
           附帶查核：use_state=False 時 c=1 八輪與 c=8 一輪選取位元相同（20/20），
           故所有已跑實驗的 chunked loop 為資訊上的 no-op，不得宣稱 sequential
           acquisition —— 已入 CLAIMS.md C-01 與憲法 §2.6。
           另立憲法 §2.5：隔離前先檢查退化。
evidence:  outputs/exp2/hier/HIER.md（結構性診斷）；tests/test_allocation_mode.py；
           docs/CLAIMS.md；docs/CONSTITUTION.md §2.5、§2.6

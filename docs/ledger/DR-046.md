# DR-046  CL 消融（Phase 0 / Phase A）的兩條裁定
date: 2026-08-26    status: ACTIVE
context:  Phase 0 交付後 PI 回填兩條裁定：(1) 測試裡自建的 `StackedLoRALinear`
          是否照准；(2) `report_dr046.py` 的 Utility Retention 欄不可讀。
options:  見下方逐條
          ※ 本行由 Cursor 補上，PI 未原文指定選項清單
ruling:   **裁定一（先例記錄）**：tests 內的 `StackedLoRALinear` **照准**，
          維持測試專用、不進 `selector/`。

            先例：**為驗證命題自建測試對照組 ＝ 允許；
                   變更 production 行為 ＝ 必須先停工詢問。**

          Phase 0 的情況屬前者：`selector/lora.py` 只有 merge-and-reset 一種模式，
          frozen-stack 是為了證明兩者等價而造的對照組，不改變主方法任何行為。

          **裁定二（指標改版）**：`report_dr046.py` 的 **Utility Retention 比值欄移除**，
          改為 **ΔUtility = 各舊 task 的 (U_final − U_own) 平均**（越高越好，
          負值 = 退化）。理由：`utility_total` **會變號**（A1 的 esca 由 +22.0
          掉到 −42.5），比值因此可能為負或絕對值極大，不是有界的保留率，
          放在同一張表上會被誤讀為百分比。差值沒有這個問題。
          同時：腳本改為**自動納入 per_slide 內實際存在的所有臂**（並提供
          `--arms` 參數），**不得寫死清單**。

          ⚠️ 分支政策：PI 決定不用分支，Phase A 起全部直接做在 main。
          Phase 0 的 `dr046-cl-ablation` 已 fast-forward 合併並刪除，
          打 tag `dr046-phase0`。
evidence: docs/DR046_TABLE.md；tests/test_merge_equivalence.py；
          tests/test_report_dr046.py；git tag dr046-phase0

---

## Phase A 落判（2026-08-31，append-only）

四臂 × 5 seeds 跑完（commit `101fb21`；`outputs/exp2/main/per_slide/`）。
配對 win count 出自 `outputs/exp2/main/EXP2.md` 的 Paired comparisons 節。

| 對照 | task-IL | class-IL | 洩漏率 | Jaccard |
|---|---|---|---|---|
| W1 − A5 | −0.96（1/5） | **−1.80（2/5）** | +1.71（1/5） | +0.06（4/5） |
| L2 − A5 | −2.06（1/5） | **−3.54（1/5）** | +2.49（1/5） | −0.08（1/5） |
| W1B − A2 | +0.94（3/5） | +2.86（4/5） | −1.59（4/5） | +0.00（2/5） |
| L2B − A2 | +1.37（4/5） | +2.04（3/5） | −1.47（3/5） | −0.00（1/5） |

**G-W1（warm-start）＝ 分裂**：class-IL 2/5，落在雜訊內；task-IL 1/5、洩漏率 1/5
亦然。依預註冊的 split 規則 → **維持現行 LoRA-from-task-1**，不改主方法。

**G-L2（single continual adapter）＝ A5 勝 4/5（class-IL）**
（L2 − A5 為 1/5，反向即 A5 − L2 為 4/5）→ **觸發 fresh-per-task 容量正當化條款**：
逐 task 重新開一組 adapter 並逐界 merge，優於單一 adapter 一路訓到底。

⚠️ **基底註記**：本輪四臂皆跑於 RESULTS_DOSSIER §4.4 的**正典 flat 基底**
（tag=main、reverse、B=8、c=1、\|M\|=512、epochs 5、λ 全 1.0），與其延伸之主表同基底，
故可直接與 A1–A5 並列比較。

**Directional 次要發現（4/5，不足以定案，但須記錄）**：
- **W1 的 selection Jaccard +0.06（4/5）** —— warm-start 的選取比 A5 更穩定，
  但**未轉化為準確率**（兩個準確率軸皆 ≤2/5）。形狀與 G4 / G3 相同（DR-043）：
  改變了「選什麼」，沒改變結果。
- **W1B − A2 的 class-IL +2.86（4/5）與洩漏率 −1.59（4/5）** —— 在**沒有**保存機制的
  條件下，warm-start 本身略優於 LoRA-from-task-1，但同樣未達 5/5。

⚠️ 四組對照在兩個準確率軸上**無一為 systematic**（DR-020 三級規則）。

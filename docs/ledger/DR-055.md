# DR-055  Evidence budget sweep：B ∈ {4, 16} 對 B = 8 的十折對照
date: 2026-09-16    status: ACTIVE
context:   主表的操作點固定在 B = 8（DR-005 的 Exp 0 預算掃描在**單任務、無 CL**
           條件下取得的峰值）。稿件把「hard budget」當成設定而非結論，審稿人會問
           連續學習下預算大小是否改變結論。PI 於 Prompt 25 裁定：在**現行主口徑**
           （`--head accumulating`、`--uold current`、兩層選擇器 hier、比較協定十折、
           seed = fold）下加跑 B = 4 與 B = 16 的兩個順序，B = 8 直接引用 DR-054
           第三階段（§11.9），不重跑。執行於新 pod（RTX 5090，`-p 58625`；
           `/workspace` 為保留磁碟，repo 與 20 GB 特徵仍在，md5 抽查 3 檔相同）。
options:   (a) 直接跑 B ∈ {4,16}，B = 8 引用既有十折
           (b) 三個 B 值全部重跑
ruling:    **(a)**，但**先做對數**：新機器上以 B = 8、fold 1 重跑一個 run，與
           `outputs/exp3/sota_ucur` 既有紀錄比最終 class-IL；**差 ≤ 1e-3 才啟動
           sweep**，> 1e-3 停下回報（B = 8 的數字必須與 Table 1 一致，否則整個
           sweep 不可用）。

           ⚠️ 對數 run 用 **fold 1、seed 1**：協定是 seed = fold，既有可比的紀錄是
           `A5_hier_rev_ucur_f1`（`--seeds 1`）。PI 指示寫的是「fold 1 seed 0」，
           但 seed 0 在此設定下沒有既有紀錄可比，照 seed = fold 執行才對得上。

           ## 設定（唯一變動 = `--budget`）

           A5、`--head accumulating`、`--uold current`、`--arch hier`、
           fold 1–10（seed = fold）、reverse ＋ forward，B ∈ {4, 16}，共 40 run。
           其餘（epochs 5、lr 1e-3、rank 4、β_s = β_u = 0.1、λ 全 1.0、
           replay_k 1、|M| 512、候選 256）一律不動。

           ⚠️ **產物目錄必須分開**：`run_exp2` 的檔名規約（`scripts/run_exp2.py`
           的 suffix 組裝）**不含 budget**，三個 B 值在同一 tag 下會互相覆蓋。
           因此 B = 4 用 `--tag sota_ucur_b4`、B = 16 用 `--tag sota_ucur_b16`，
           B = 8 留在既有的 `sota_ucur`。per_slide 的 `B` 欄位另存實際預算，
           報表以目錄區分、並以該欄位自檢。

           ## 判準（凍結）

           * 對數：|新機器 B=8 最終 class-IL − 既有紀錄| ≤ 1e-3 → 啟動；否則停止。
           * 報告：兩個 B 值 × 兩順序的 ACC／Masked／Forgetting（mean ± sd，10 折）、
             逐折配對 **B=4 − B=8** 與 **B=16 − B=8**（同折相減，報均值差與較佳折數）。
           * **只報告**：不宣稱最佳預算、不改主表的 B = 8 設定、不改既有產物。
             數字進 `outputs/exp3/B9.md` 與 dossier §11.10，納入 PAPER_ARTIFACTS。
evidence:  本卡（預註冊，先於 sweep）；對數結果、outputs/exp3/{sota_ucur_b4,sota_ucur_b16}、
           logs/pod/、dossier §11.10 於後續 commit 補記於本卡末尾（append-only）。

           ## 補記 2026-09-16（對數通過；sweep 完成，40 run 零失敗）

           對數：新 pod 的 B = 8、A5 hier、fold 1 重跑與 `outputs/exp3/sota_ucur` 既有
           紀錄 **569 筆逐位元相同**（class-IL 差 0.00e+00）→ 啟動 sweep。
           （途中修一處環境問題：新 pod 的 `/` 是全新 overlay，舊機建在
           `/Users/aaron/research/can_dataset` 的資料符號連結不存在，configs 走絕對路徑
           故第一次對數 FileNotFoundError；重建連結後通過，與程式無關。）

           結果（ACC；十折）：B=4 reverse 0.816／forward 0.759；B=8 0.834／0.837；
           B=16 0.829／0.847。配對：B=4 − B=8 reverse −0.0180（4/10）、forward
           **−0.0784（1/10）**；B=16 − B=8 reverse −0.0047（5/10）、forward +0.0098（6/10）。
           只報告：B = 8 以下明顯受限（正向尤甚，且折間 sd 由 0.027 放大到 0.091），
           以上沒有可辨識增益；主表操作點不動。dossier §11.10、outputs/exp3/B9.md。

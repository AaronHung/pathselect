# DR-049  投稿模板：官方 ICASSP 2027 kit
date: 2026-09-08    status: ACTIVE
context:   稿件原先用的是 8/30 取得的 spconf.sty / IEEEbib.bst。PI 於 v0.89m
           換上官方 2027 kit，需確認換版沒有帶入任何實質變動、也沒有遺漏。
options:   (a) 直接取代不核對
           (b) 逐檔核對內容差異後取代，舊版存查
ruling:    **(b)**。核對結果如下，**兩個檔案都沒有實質改動**：

           | 檔案 | 結果 |
           |---|---|
           | `spconf.sty` | 與 8/30 版**除換行符外位元相同**（252 行全部只是 CRLF↔LF） |
           | `IEEEbib.bst` | **內容零變動**（git 未偵測到差異，僅 mtime 更新） |

           舊版 `spconf.sty` 以 `paper/versions/spconf_pre2027.sty` 存查，
           已驗證與換版前 git 中的檔案位元相同。

           ⚠️ **因此「換上 2027 kit」在排版上是 no-op** —— 官方沒有改內容。
           這件事本身值得記錄：日後若有人懷疑版面問題出在模板換版，
           這裡有逐檔核對可以直接排除。

           規定核對表（本輪實測值）逐項列於 commit 訊息，摘要：
           `\documentclass{article}` + `\usepackage{spconf,...}`、
           `\ninept` 在（1 處）、`\bibliographystyle{IEEEbib}`、
           `\section*{Compliance with Ethical Standards}`（不編號，符合慣例）、
           main.pdf **7 頁**。

           ⚠️ **頁數需 PI 對照 CFP 確認**：7 頁是否符合 ICASSP 2027 的頁數上限
           （常見規定為內文 4 頁 + 參考文獻），本輪未查證徵稿啟事，
           **不做判斷也不擅自裁切**。
evidence:  paper/spconf.sty、paper/IEEEbib.bst、paper/versions/spconf_pre2027.sty；
           核對指令與輸出見本輪 commit 訊息

---

## Fig.1 v1_0（無 fallback）入庫紀錄（2026-09-08）

`paper/figures/` 只保留 `fig1_architecture_v1_0.{pdf,png,svg}`（PI 新匯出）；
`v2_0` 三檔以 `git rm` 移除，**依裁定不進 archive**（PI 自行備份）。
`main.tex` 直接指向 v1_0，**不使用 `\IfFileExists` fallback**
（實測 `IfFileExists` 命中 0、`v2_0` 命中 0）。

### 匯出守門（四道，全過）
| 檢查 | 結果 |
|---|---|
| 不含 `Equivalence` / `Segmentation` / `CURRENT DIAGNOSIS` | ✅ 三者皆 0 |
| 含 `Utility Preservation`(1) / `Grouping`(2) / `All Benchmark class prompt`(1) / `candidate`(1) / `feature store`(1) | ✅ 五項全有 |
| 三檔同一次匯出 | ✅ 跨度 **43 秒** |
| PDF 字型全內嵌 | ✅ 30 個字型，`emb=no` 為 0、Type 3 為 0 |

⚠️ 文字檢查在**原始 SVG 與去標籤後各做一次** —— SVG 的文字可能被 `tspan`
切開，只查原始字串會漏檢。

### ⚠️ 一個被守門抓到的真問題：main.pdf 曾是舊圖

第一次送來的 `main.pdf`（12:59:12）**早於**新圖（21:01:35）八小時，
`pdftotext` 抽全文比對確認它嵌的是**舊的 v2_0**：
含 `CURRENT DIAGNOSIS`（只在 v2_0）、缺 `All Benchmark class prompt`（只在 v1_0）；
且 `main.tex` 本身不含前者，故該字串只可能來自嵌入的圖。已停下回報，
PI 重新編譯後（21:44:30）五項判別字串全部正確才入庫。

**自檢的漏洞已修**：原本只比對 `main.pdf` vs `main.tex`（兩者皆 12:59 → 通過），
**沒有比對圖檔**。今後的時間戳檢查改為
**`main.pdf` 必須晚於 `main.tex` 與 `figures/*.pdf` 全部**。

### 圖上兩處待最後匯出修正
1. **`L_KD` 第二項缺 σ** —— 蒸餾式子的第二項應有 softmax 記號。
2. **class prompt 應為複數** —— 標籤文字的單複數。

兩者**不影響本輪入庫**（不改變任何數值或結構），列為下次匯出時一併處理。

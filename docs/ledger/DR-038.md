# DR-038  L_sem 的措辭修正與範圍限定（取代 DR-036）
date: 2026-08-24    status: ACTIVE
context:   DR-036 寫「HistoSelect 的貢獻在於分組結構而非語意先驗」，該句為循環論證 ——
           我們正是在「分組結構壓過 patch 分數」的階層架構裡測 patch 層先驗。
           且「prior 與選取架構正交」為未查 code 的臆測，經查證不成立。
options:   (a) 修改 DR-036 內文 (b) DR-036 改 status，由新卡承載修正後的措辭
           ※ 本行由 Cursor 依 PI 指示補上（規則 1：已寫的卡不改內文）
ruling:    (b)。修正後的措辭：**「在階層架構下，語意先驗的移除不損害準確率。」**
           刪去「HistoSelect 的貢獻在於分組結構而非語意先驗」（循環）。
           其餘 DR-036 的裁定不變：L_sem 不得宣稱改善準確率；降級為弱正則；
           DR-007 選 discriminative 的理由（避免 simple similarity）仍成立且獲新支持；
           須同時報 max_sim 洩漏率最低（9.74）。
           範圍限定的機制依據：L_sem 只錨定 patch 分數 s，flat 下 s 單獨決定選取
           （完整槓桿）、階層下 r 先定各組名額 s 只在組內排序（槓桿被稀釋），
           故階層下的 null 不可外推到 flat。
evidence:  outputs/exp2/prior/PRIOR.md；selector/train.py::l_sem（單項實作）

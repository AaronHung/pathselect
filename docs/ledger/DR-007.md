# DR-007  L_sem 主線 = discriminative prior
date: 2026-08-21    status: ACTIVE
context:   semantic prior 有三種候選；max-sim 實質上就是 simple similarity，
           正是老師批評「只是相似度」的那一點，不宜當主線。
options:   none / max_sim / discriminative
ruling:    主線 discriminative：p_i = 1 − H(softmax(cos/T))/log C，
           與 v9 的 entropy 輸入延續，且不會退化成單純相似度。
           三種全部實作、可用參數切換；prior 一律看**全部** candidate class（assert 強制）。
evidence:  selector/priors.py（assert_full_class_space）；tests/test_priors.py；
           commit 08a5af4

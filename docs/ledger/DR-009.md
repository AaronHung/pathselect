# DR-009  --group-grad 預設 ste_allocation
date: 2026-08-21    status: ACTIVE
context:   r_j → softmax → ×c → largest-remainder 取整不可微，L_diag 的梯度到不了
           F_g。原契約沒有考慮到取整會截斷梯度。
options:   none（契約字面）/ ste_allocation / 靠 L_util 之後補監督
ruling:    預設 ste_allocation，以契約**意圖**為準：none 模式下 F_g 在整個
           within-task 訓練中等於固定的隨機函數，那樣得到的「hierarchy 沒用」
           是構造保證的 null 而非實驗結果。none 降級為 ablation。
           forward 位元不受影響（a_j / a_j.detach() 恆等於 1）。
evidence:  selector/rounds.py（GROUP_GRAD_MODES）；tests/test_rounds.py
           （預設有梯度 / none 無梯度 / forward 位元相同三條）；commit eea09ce

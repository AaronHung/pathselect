# DR-031  E1 記憶體曲線以階層版完整重跑（方案 A），排在最後
date: 2026-08-23    status: ACTIVE
context:   主線改為階層（DR-029）後，E1 的記憶體曲線（2× 效率）仍是 flat 版量測的。
           實測每輪 28.4 分（flat 與 hier 相同），完整重跑 40 輪約 18.9 h。
options:   (a) 完整 40 輪 (b) 只補 2× 主張所需 15 輪 (c) 只補兩端 20 輪
           (d) 3 seeds 完整 24 輪
ruling:    (a)。不採 (c)：主表階層、曲線 flat 就是剛修過的圖文不一致，只是換了位置；
           且 hier-A3 − flat-A3 = −3.11 pp 已證明 replay 在階層下的行為與 flat 不同，
           曲線不可假設可移植。時程充裕（到 8/31 尚有 8 天）。
           **排在最後**：它是最長的單一批次，但結果不改變方法本體、只重建防禦論述；
           會改變方法或主張的實驗先跑完，萬一中途出事，損失的是防禦強度而非方法定案。
           額外要求：沿用 G1' 的 |M|=512 存檔；**每個 |M| 都報結構性指標**，
           某容量下退化即標明該格判讀無效；格式與 flat 版一致以便並列；
           另產出 flat vs hier 的曲線對照。
evidence:  待 outputs/exp2/memory_hier/MEMORY_HIER.md；scripts/report_memory_hier.py

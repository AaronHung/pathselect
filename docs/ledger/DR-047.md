# DR-047  replay 預算與記憶庫 policy 的敏感度（選配，未跑）
date: 2026-09-04    status: PARKED
context:   `replay_k` 固定為 1、記憶庫汰換固定為 reservoir，兩者從未做過敏感度掃描。
           E1 的記憶體曲線只掃 \|M\|，沒有掃「每次抽幾筆」與「換不換 policy」，
           所以「replay 強度」這個混淆因子是靠「replay_k 固定且與 \|M\| 無關」
           排除的，不是靠實測（SEEDS S-01）。
options:   (a) 跑 replay_k ∈ {0,1,2,4} × 記憶庫 policy 變體
           (b) 擱置，於擴充版再補
           ※ 本行由 Cursor 補上，PI 未原文指定選項清單
ruling:    (b) **擱置**。理由與範圍逐字取自 PI 已提交的兩份文件：

           - `docs/PathSelect大全_PI_Review手冊.md` L64：
             「**DR-047**（選配未跑）｜replay 抽幾筆、記憶庫換不換 policy｜—｜
             「比例多少」的敏感度備援」
           - `docs/四人工作清單_0831_to_0916.md` C6：
             「（選配，僅 PI 出差期間機器閒置且核准）DR-047 replay_k 掃描；否則跳過」
             （9/4–9/6，前置＝PI 核准）
           - 同檔 F12：「（投稿後）… DR-047 正式化」（9/16 後）

           論文的處置：replay 預算的敏感度**延到擴充版**，正文不宣稱。

           ⚠️ **本卡由 Cursor 依上述兩份 PI 文件補記**，PI 未原文口述卡片內容。
           建卡的直接原因是 `tests/test_ledger.py` 強制「DR 編號不得有缺口」——
           PI 在 Prompt 5 直接指定新卡為 DR-048，而 047 一直只存在於工作清單裡。
           INDEX 明訂「補記早先的決策是允許的」，故以 PARKED 補位而非虛構裁定。
           **實際要跑時請由 PI 覆寫本卡的 ruling**（append 一段修訂即可）。
evidence:  docs/PathSelect大全_PI_Review手冊.md L64、L143；
           docs/四人工作清單_0831_to_0916.md C6、F12；SEEDS.md S-01

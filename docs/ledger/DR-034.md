# DR-034  報告腳本的 fixture 必須涵蓋所有遍歷維度
date: 2026-08-24    status: ACTIVE
context:   同類錯誤第四次（ALLOCATION_MODES、diag、zsh word splitting、本次 KeyError）。
           report_order_dependence.py 的 KeyError: 'final_task_il' 只在「兩個 order
           且各 arm 的 seed 數不齊」時才踩得到 —— main order 只有 A3/A5 補到 5 seeds，
           其餘仍 3 seeds，而共同 seeds 是在 order 層取聯集算的。
           §3.6 的最小資料測試全綠卻擋不住，因為單 order fixture 下該行不可達。
options:   (a) 只修這次 (b) 補強 §3.6：fixture 必須涵蓋所有遍歷維度
ruling:    (b)。憲法 §3.6b：報告腳本的測試 fixture 必須涵蓋該腳本實際會遍歷的所有
           維度（order / arch / prior / arm / 記憶體容量 / seed 數不齊）；
           新增測試前先以 mutation check 確認會紅。
           附帶條款：**跨 tag 蒐集必須過濾所有會改變語意的欄位**，缺欄位視為舊語意 ——
           同日 report_prior.py 只過濾 arch 未過濾 allocation，把 G1 的退化紀錄
           當成 discriminative 主線臂，產出「主線遠差 −17.5 pp」的假結論。
           另：§3.7 的狀態檔必須**主動讀**，每次回報前先報所有 job 狀態。
evidence:  outputs/exp2/pipeline.log（stage 3 KeyError）；
           outputs/exp2/prior/PRIOR.md（修正前後差異）；tests/test_report_scripts.py

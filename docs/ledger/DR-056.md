# DR-056  候選級 replay 探針（決策記錄在 `exp/candidate-replay` 分支）
date: 2026-09-18    status: ACTIVE
context:   老師 2026-09-17 問 replay buffer 的大小。稽核發現 entry 雖不存 patch feature
           （CONTRACT-3 成立），但 replay 執行期會讀**整個 slide 的特徵檔**，被記憶庫收過的
           slide 聯集等於訓練集 100%（特徵庫 19.55 GiB）。於是開了一個隔離分支探針：
           replay 只載入每筆快照的 ≤256 個候選特徵。

           ⚠️ **本卡在 `main` 上是指向性的佔位卡。** 完整的卡（含 CONTRACT-3 欄位變更、
           磁碟側倉的實測 bytes、四個 probe run 的表與判準、第一批因側倉互相覆寫而作廢的
           證據）在分支 **`exp/candidate-replay`** 的 `docs/ledger/DR-056.md`，
           HEAD `9cc3406`。該分支**未 merge 回 main**，所以 main 這一線看不到它。

           本卡存在的理由與 DR-047 相同：`tests/test_ledger.py::test_dr_numbers_have_no_gaps`
           要求編號連續。不在此重述內容，以免兩份卡各自漂移。
options:   (a) 在本分支重寫一份完整的 DR-056（兩份卡會各自漂移）
           (b) 放一張指向原卡的佔位卡（DR-047 判例）
           (c) 把本批的卡改編號為 056（會與另一分支的 DR-056 撞號）
ruling:    **(b)。** 指向 `exp/candidate-replay` 的原卡，不重述、不改寫。

           原卡的裁定摘要（2026-09-18，PI）：**放棄候選級 replay，不做自足性驗證，
           分支與產物保留、不刪除、不 merge 回 main。** 理由：reverse 三條判準同時未過，
           Masked 掉 4.46 pp 代表當前任務的證據品質也下降；兩序 Forgetting 都明顯升高。
           實測：單一 replay 步驟少讀 12.5 倍、|M|=128 的 buffer 僅 64.2 MiB（完整特徵庫的
           0.45%）、每個 run 快約 36 分鐘；代價是 ACC −1.27～−3.52 pp、Forgetting +3.62～+5.78 pp。
evidence:  分支 `exp/candidate-replay` HEAD `9cc3406`：`docs/ledger/DR-056.md`（完整卡）、
           `outputs/exp4/DR056_PROBE.md`、`outputs/exp4/BATCH1_INVALID.md`、
           `outputs/exp4/MACHINE.md`、`docs/RESULTS_DOSSIER.md` §11.11。
           本卡不重複收錄那些數字 —— 要引用請直接讀該分支。

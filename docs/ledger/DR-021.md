# DR-021  架構一致性：CL 主線改用階層選取器
date: 2026-08-24    status: ACTIVE
context:   Exp 2 七臂皆為 flat（L3b），但定稿架構圖中央為 Group→Patch 階層。
           標題含 Hierarchical 而主表為 flat 會構成致命不一致。成因是 8/22 將
           L5/L6 後移的排程決定 —— 實驗優先序正確，但未預見方法本體會與圖脫節。
options:   (a) 以階層重跑 CL 主線（10 輪 ~4.5h） (b) 改圖改標題為 flat
           (c) 誠實聲明主表為 flat 變體、階層降為 component ablation
ruling:    (a)，優先序置於 F3 之前。4.5 小時買回標題與架構圖的一致性，高於多一個
           備援臂的價值。G1 判準已 pre-register：階層 ≥ flat 或在雜訊內皆採用階層
           為主線；僅在顯著劣於 flat 時由 PI 重新裁定。G1 只開階層，不同時開
           q_tau 與 state，以維持可歸因（Gate 1 教訓）。
evidence:  待 outputs/exp2/hier/HIER.md；tests/test_arch_switch.py（唯一差異為
           hierarchy 的 8 條把關，含 mutation check）

# DR-022  group-level distillation 從未被測試；G1 增臂隔離
date: 2026-08-24    status: SUPERSEDED-BY DR-035
context:   G0 實測顯示 flat 模式下 F_g 對選取零影響（替換為擾動網路後選取位元相同），
           故 L_KD 的 group 項對所有已回報指標影響為零。架構圖 Panel I 畫有該項。
options:   (a) 撤回 group-level 主張、只寫 patch-level (b) 以 G1 首次測試該機制並增臂隔離
ruling:    (b)。既有結果有效不重跑；但 EXP2.md 須加誠實聲明「已回報的 KD 效果僅來自
           patch-level」。G1 增加 hier-A5nG 臂（5 seeds）以隔離 group 項
           （`l_kd(..., group_weight=0)`，完全不計算、r_new 不進計算圖）。
           若無作用則論文誠實寫成 patch-level 並列入 limitation，不調 λ 搶救。
           另記：A3 在 flat 下 F_g 無梯度停在初始值，hier 下會實際訓練，
           故 hier-A3 為更強 baseline，不得跨模式直接比較。
evidence:  G0 F_g 替換實驗（tests/test_arch_switch.py）；待 outputs/exp2/hier/HIER.md

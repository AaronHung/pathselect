# paper/versions/

**常規**：`paper/` 根目錄只留 **`main.tex` 一個 `.tex`**（當前投稿版）。
所有備份與凍結長版住這裡。

| 檔案 | 說明 |
|---|---|
| `extended_master.tex` | 凍結長版（Phase B 時的完整版母本） |

⚠️ 規格提到的 `main_bk_20260831_phaseB.tex` 在建立本目錄時**不存在於工作區**，
故未移入。之後補上時直接 `git mv paper/main_bk_*.tex paper/versions/`。
